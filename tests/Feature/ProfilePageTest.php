<?php

/* namespace Tests\Feature;

use Tests\TestCase;
use App\Models\User;
use Illuminate\Foundation\Testing\RefreshDatabase;

class ProfilePageTest extends TestCase
{
    use RefreshDatabase;

    public function testProfilePageLoadsCorrectlyForAuthenticatedUser(): void
    {
        // Crea un usuario
        $user = User::factory()->create();

        // Simula que el usuario está autenticado
        $response = $this->actingAs($user)->get('/profile');

        // Verifica que la respuesta tenga un estado 200
        $response->assertStatus(200);

        // Verifica que el contenido de la página contiene el nombre del usuario
        $response->assertSee($user->name);
    }
} */

